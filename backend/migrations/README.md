# Schema is created with SQLAlchemy metadata on startup (create_all).
# For PostgreSQL production, generate Alembic revisions from app.models and apply:
#   alembic revision --autogenerate -m "init"
#   alembic upgrade head
# Keep seed data in backend/seed/seed_data.py
